= simple_form_for @listing do |f|
  = f.input :title
  = f.input :description
  = f.input :quantity
  = collection_select(:listing, :item_id, Item.all, :id, :kind)
  = f.input :image
  = f.button :submit